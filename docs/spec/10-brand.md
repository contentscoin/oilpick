# 10 — 브랜드: 오반장 (OBJ)

2026-07-16 CEO 지시로 확정한 리브랜딩. **오일픽(OilPick) → 오반장(OBJ)**.
이 문서가 브랜드 표기의 단일 진실이다.

## 결정 기록
| # | 결정 | 확정 |
|---|---|---|
| B1 | 서비스명 | 한글 **오반장** / 영문 **OBJ**(O-Ban-Jang 이니셜). 로크업: "오반장 OBJ" 또는 "OBJ — 오반장" |
| B2 | 영문 부제(태그라인) | **"Oil pickup, paid on the spot"** — 핵심 가치(수거+현장 지급) 직서술. 짧은 변형 "Paid on the spot"(공간 협소 시). 대안 후보로 "Your used-oil captain"(반장 콘셉트 강조)을 보류 기록 |
| B3 | 캐릭터 | **오반장 캐릭터** — 기름 방울 몸체(앰버 그라디언트)에 노란 안전모와 초록 "반장" 완장을 두른 친근한 현장 반장. 미소+홍조, 플랫 스타일(브랜드 토큰 색상만 사용). 1차 구현은 packages/ui `Mascot`(순수 SVG — 외부 에셋 無, 크기 prop). **정식 아트(일러스트·앱 아이콘·스플래시)는 로고 발주 시 이 콘셉트 기준으로 제작**(launch-plan Phase 0 로고 항목과 병합) |
| B4 | 적용 범위 | **유저 노출 계층만 즉시 교체**: 앱 타이틀(index.html)·런처 이름(capacitor appName)·화면 헤더·공유 카피·문서 헤드라인. **기술 식별자는 유지**: `@oilpick/*` 패키지·`kr.oilpick.*` appId·`oilpick-user://` 스킴·`oilpick_referral_code` 스토리지 키·저장소/Vercel 프로젝트명·`app.oilpick.kr` 도메인 기본값 — 스토어 등록 전 일괄 변경 여부는 도메인 확보(launch-plan) 시 별도 결정(변경 시 네이티브 재생성·딥링크·기저장 코드 마이그레이션 수반) |
| B5 | 스펙 이력 문서 | 00~09 스펙 본문의 "OilPick/오일픽" 표기는 이력 그대로 보존(코드명 참조). 신규 문서·유저 카피만 오반장/OBJ |
| B6 | 브랜드 그래픽 에셋(1차) | **마스코트 파생 마크**(방울+안전모 — 완장·홍조는 소형 가독성 문제로 생략)로 파비콘(SVG)·apple-touch-icon(180², 크림 `accent.light` 배경)·OG 이미지(1200×630, 브랜드 그린 그라디언트+크림 카드+OBJ 워드마크+B2 태그라인)를 3앱 공통 제작. OG 이미지 텍스트는 **라틴만**(한글은 og:title/description 텍스트로 전달 — 크롤러가 렌더). OG 메타는 user 앱만(공유 발생 지점 = 추천 랜딩 `/ref/:code` 포함 전 경로); `og:image` 절대 URL은 현 Vercel 프로덕션 도메인(B4 준용), **도메인 확보 시 교체**. 정식 아트 발주 시 전체 교체(B3) |

## 마스코트 사용처 (1차)
- 【U】 추천 랜딩 `/ref/:code` 상단(이모지 ♻️ 대체 — 첫 브랜드 접점).
- 【U】 `/dev-ui` 쇼케이스(육안 확인).
- 【공통】 파비콘·apple-touch-icon·OG 이미지(B6 파생 마크 — `apps/*/public/`).
- 후속(선택): 온보딩 첫 슬라이드, EmptyState 일러스트 변형, 앱 아이콘(정식 아트 후).

## 남은 브랜드 작업 (launch-plan과 병합)
- [ ] 상표 출원 재검토: "오반장 / OBJ"(기존 OilPick 항목 대체 — 35·39·42류)
- [ ] 도메인: obj 계열 또는 obanjang.kr 확보 검토 → 확보 시 B4 기술 식별자 일괄 변경 결정
- [ ] 정식 캐릭터 아트·로고·앱 아이콘/스플래시 발주(B3 콘셉트 기준)
