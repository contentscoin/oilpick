# 10 — 브랜드: 폐유 (payou)

2026-07-23 CEO 지시로 재확정한 리브랜딩. **오반장(OBJ) → 폐유(payou)**.
(이력: 오일픽(OilPick) → 오반장(OBJ, 2026-07-16) → **폐유(payou, 2026-07-23, 현행)**.)
이 문서가 브랜드 표기의 단일 진실이다. 유저 노출 텍스트에는 **폐유/payou**만 쓴다.

## 결정 기록 (현행 = 폐유/payou)
| # | 결정 | 확정 |
|---|---|---|
| B1 | 서비스명 | 한글 **폐유** / 영문 **payou**. "폐유(廢油, 폐식용유)"의 발음이 영어 **"pay you"**(현장에서 값을 지급)로 이어지는 워드플레이가 핵심. 로크업: "폐유 payou" |
| B2 | 영문 부제(태그라인) | **"We pay you for used oil"** — payou 워드플레이 + 핵심 가치(폐유를 값으로 지급) 직서술. 짧은 변형 후보: "Used oil, we pay you" / "Your oil, paid." (대안은 아래 태그라인 후보 참조) |
| B3 | 로고/캐릭터 | **제리캔(기름통) 마크 + 'P'**(CEO 제공 로고, 2026-07-23): 딥그린 라운드 스퀘어 안에 크림색 기름통, 통 정면에 'P', 금색 캡. 워드마크는 붓글씨체 "폐유" + 하단 소문자 "payou" + 금색 브러시 언더라인. **정식 로고 에셋(제리캔+P)은 CEO 제공 이미지가 원본** — 앱 아이콘·스플래시·파비콘에 이 원본을 반영(현재 인앱 `Mascot`(오반장 방울 캐릭터)은 신 로고와 컨셉 불일치 → B6·후속에서 교체) |
| B4 | 적용 범위 | **유저 노출 계층만 즉시 교체**: 앱 타이틀(index.html)·런처 이름(capacitor appName)·화면 헤더·공유 카피·문서 헤드라인 = 폐유/payou. **기술 식별자는 유지**: `@oilpick/*` 패키지·`kr.oilpick.*` appId·`oilpick-user://` 스킴·`oilpick_referral_code` 스토리지 키·저장소/Vercel 프로젝트명(`oilpick-*`)·`app.oilpick.kr` 도메인 기본값 — 스토어 등록 전 일괄 변경 여부는 도메인 확보(launch-plan) 시 별도 결정 |
| B5 | 스펙 이력 문서 | 00~09 등 스펙 본문의 "OilPick/오일픽"·"오반장/OBJ" 표기는 이력 그대로 보존(작성 시점 코드명·브랜드 참조). 신규 문서·유저 카피만 폐유/payou |
| B6 | 브랜드 그래픽 에셋 | ✅ **폐유 제리캔+P 로고 반영 완료**(2026-07-23, CEO 제공 로고 SVG 재현): 파비콘(SVG)·apple-touch-icon(180²)·OG(1200×630)를 3앱 공통 제리캔+P 마크로 교체. 인앱은 `packages/ui BrandMark`(제리캔+P 순수 SVG) — 추천 랜딩 로크업·dev-ui에서 사용. OG는 제리캔 카드 + 폐유(한글) + payou + 태그라인. `og:image` 절대 URL은 현 Vercel 도메인(B4), 도메인 확보 시 교체. (기존 방울 `Mascot`은 보조 캐릭터로 잔존 — 로고 아님.) 로고 팔레트: 딥그린 `#1B5E3F`·크림 `#F4EEDF`·골드 `#C9A24B` |

## 태그라인 후보 (B2 — CEO 선택용)
- **We pay you for used oil** (현행 반영값 — 명확 + payou 펀)
- Used oil, we pay you (짧고 리듬감)
- Your used oil, paid on the spot (기존 "현장 지급" 가치 계승)
- We pay you, on the spot (초간결)

## 남은 브랜드 작업 (launch-plan과 병합)
- [ ] **로고 에셋 반영**: CEO 제공 제리캔+P 로고를 파비콘(SVG)·apple-touch-icon(180²)·OG(1200×630)·
  앱 아이콘/스플래시에 적용(B6). 인앱 `Mascot`(방울 캐릭터) 사용처(추천 랜딩·dev-ui) 교체 검토.
- [ ] 상표 출원: "폐유 / payou"(35·39·42류). ⚠️ "폐유"는 일반명사(廢油)라 **식별력·등록 가능성 사전
  검토 필요**(로고 결합 출원·payou 워드마크 병행 등 변리사 상담).
- [ ] 도메인: payou 계열(payou.kr/.co.kr 등) 확보 검토 → 확보 시 B4 기술 식별자 일괄 변경 결정.
